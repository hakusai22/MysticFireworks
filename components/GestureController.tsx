// This component has been disabled as per user request to remove camera functionality.
// Keeping the file placeholder to avoid build errors if referenced elsewhere, but removing active code.
import React from 'react';

const GestureController: React.FC<any> = () => {
  return null;
};

export default GestureController;